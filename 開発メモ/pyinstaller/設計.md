### スクリプト設計
    ```js
pyinstaller --onefile --add-data "myapp/templates;myapp/templates" --add-data "myapp/static;myapp/static"  --add-data "seed;seed" --hidden-import flask --hidden-import jinja2 app.py
    ```

    ```js
pyinstaller --onedir --add-data "myapp/templates;templates" --add-data "myapp/static;static" --add-data "seed;seed" --add-data "backup;backup" --hidden-import flask --hidden-import jinja2 app.py
    ```

```js
pyinstaller --onedir --add-data "myapp/templates;myapp/templates" --add-data "myapp/static;myapp/static" --add-data "seed;seed" --add-data "backup;backup" --hidden-import flask --hidden-import jinja2 app.py

// いま
pyinstaller --onedir --add-data "C:\\z\\app\\stocktrack\\myapp\\templates;myapp/templates" --add-data "C:\\z\\app\\stocktrack\\myapp\\static;myapp/static" --add-data "C:\\z\\app\\stocktrack\\myapp\\seed;seed" --add-data "C:\\z\\app\\stocktrack\\backup;backup" --hidden-import flask --icon=box-seam.ico --hidden-import jinja2 app.py

// 正式
pyinstaller --onedir --add-data "myapp/templates;templates" --add-data "myapp/static;static" --add-data "instance;instance" --add-data "myapp/seed;seed" --hidden-import flask --icon=box-seam.ico --hidden-import jinja2 --log-level=DEBUG app.py


```
pyinstaller --onefile --add-data "myapp/templates;templates" --add-data "myapp/static;static" --add-data "instance;instance" --add-data "seed;seed" --hidden-import flask --hidden-import jinja2 app.py
```

### メモ
・testファイルは含まない
・.dbファイルは外だし
・logファイルは外だし
・backupフォルダは外だし


    project_root/
    ├── app.py                     # メインのエントリーポイント
    ├── myapp/
    │   ├── templates/             # Flaskのテンプレート
    │   ├── static/                # Flaskの静的ファイル
    ├── seed/
    │   ├── shelf.csv              # CSVデータファイル
    │   ├── zone.csv
    │   └── cell.csv
    ├── backup/                    # バックアップ用ディレクトリ
    └── other_files_or_dirs/       # その他のファイルやディレクトリ
